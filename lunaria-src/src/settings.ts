/**
 * Ajustes en vivo de la simulación, compartidos entre la UI (HTML) y la
 * física (dentro del Canvas). Objeto mutable simple para evitar re-renders.
 */
export const settings = {
  gravity: 14, // fuerza de la gravedad esférica (m/s² hacia el centro)
}
